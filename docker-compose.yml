version: '3'
services:
  app:
    build: .
    container_name: disaster-api-app
    ports:
      - "3000:3000"
    depends_on:
      - redis
    environment:
      - NODE_ENV=production
      - REDIS_HOST=ptpofficialxd.redis.cache.windows.net
      - REDIS_PORT=6379
      - REDIS_PASSWORD=kgbkTm5nFaRaO1b52rzoO4VZjzo2Ja0BhAzCaAYF82g=
  redis:
    image: redis:alpine
    container_name: disaster-api-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

volumes:
  redis_data: